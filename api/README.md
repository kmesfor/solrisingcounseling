This folder **SHOULD NOT** be shipped together with the `src` or `build` folders!

This project should be run **SEPERATELY** from the main website

Run the project with the command `node api/index.js` or `node index.js`